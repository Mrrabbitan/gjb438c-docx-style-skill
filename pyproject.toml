[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "gjb438c-docx-style-skill"
version = "0.1.0"
description = "Codex skill for GJB438C-style DOCX template generation and formatting audit."
requires-python = ">=3.9"
dependencies = [
  "python-docx>=1.1.2",
  "PyYAML>=6.0.1",
]

[tool.setuptools]
py-modules = []
