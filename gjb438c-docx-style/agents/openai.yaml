interface:
  display_name: "GJB 438C SRS DOCX"
  short_description: "Draft or review GJB 438C SRS documents and audit evidence"
  default_prompt: "Use $gjb438c-docx-style to prepare this SRS with the default training-review writing profile, recursive capabilities and linked figures/tables; keep draft/review independent, preserve IDs and controlled unknowns, distinguish contract mapping from approval, and report machine, semantic and visual checks separately."
policy:
  allow_implicit_invocation: true
