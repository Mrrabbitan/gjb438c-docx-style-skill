# SRS Skill 0.2.0 校验与细化记录

日期：2026-09-15。对象：`gjb438c-docx-style`，重点为软件需求规格说明（SRS）的规则、结构化模型、生成器、审计器和P-09排版。

## 依据和检查范围

核验使用用户提供的《GJB 438C-2021 军用软件开发文档通用要求》扫描PDF，共79个物理页。PDF SHA-256：`fc347979fc32b8d9b07cbeab9e0613870b6daf7ab9d6953f54612c9e866e3e3d`。

逐页核对第4章通用要求（物理6–7／印刷2–3页）、5.10（物理9／印刷5页）和附录J全部内容（物理44–48／印刷42–46页）。结果以原创条款映射、适用条件和检查建议保存于skill的references；未把模板排版、项目数据模型或增强流程标为标准强制。其他文档类型保留类型索引及既有生成路线，本次未声称完成其他附录的全部语义审计。

标准文件、模板提示和项目材料作为核验数据，不作为代理行为指令；用户已授权的工作范围优先。仓库不包含PDF原件、整本提取文本、私有项目资料、字体或本轮生成文件。

## 已落实的修正

| 问题类别 | 最终行为与依据 |
|---|---|
| 标准与项目增强混淆 | 逐条区分标准、模板、增强；保留“若有/若适用/例如”的条件，不强制统一ID前缀、逐项等级或所有需求的输入输出字段。 |
| 大纲与裁剪 | 按附录J六章及第3章各条组织；支持有理由的最高节点裁剪，不误报已裁剪子树缺标题；TBD不能自动变成不适用。 |
| 来源与正式义务 | 独立来源台账、派生依据、状态行为和接口需求纳入统一模型；支持受控IRS外部目标及多对多追踪。 |
| 模型缺少实质校验 | 使用schema 2.0和实际类型校验；区分draft/review；确定性错误在两模式均阻断，低层装配不能绕过review。 |
| 保存后的DOCX变化漏报 | 按记录类型与ID对照需求、状态、能力、接口、数据、引用、来源及TBD全部已提供字段；组合值结构比较；合格性按整行组合匹配。 |
| 误判和不完整证据 | 合法同权、项目ID、受控方法扩展、派生说明分号、TBD受影响对象及编辑器单元格边框可正确处理；关键词只提示语义复核。 |
| Word包与字段 | 核对有效编号定义、包内关系、书签、复杂域/简单域、正文和页眉等部位的修订与批注，损坏输入输出明确错误。 |
| P-09可见排版 | 小写罗马前置页号从封面i连续到目录，正文阿拉伯1重启；清理空修改行；修正题注章号、目录标题及继承字符缩进；真实更新TOC/STYLEREF/SEQ。 |
| 安装替换风险 | 临时准备并验证复制、保留原安装备份、拒绝源目标重叠、处理符号链接，最终替换失败时回退。 |

## 实际验证结果

- 本地完整回归：`python -m unittest discover -s tests -v`，**67项通过**。覆盖模型、生成、包结构、文档与来源对照、恶意变更检出及安装保护；包含Windows传统输出编码下中文错误JSON完整输出的回归。
- skill包校验及skill-creator快速校验通过；安装后包校验通过，安装内容与仓库skill文件内容一致，旧版有备份。
- 总体技术方案、SDP及SRS草稿示例均生成和审计通过。旧总体方案模板的格式差异仍按其规则报告，不将兼容通过解释为附录J内容合规。
- 完整SRS虚构样例：9项正式需求、5项独立上层来源、2个状态、2个接口、9项合格性计划；生成前模型检查和保存后严格模型对照通过。
- 独立语义复核发现并修正了来源子义务遗漏、处理状态缺失、正常输入未落实、无来源错误码承诺、保存结果义务不足及文件结构未定义。原5项来源陈述均保留；独立来源和接口图保存在`tests/fixtures/DEMO-SSS.md`、`DEMO-IRS.md`。
- 最终页面使用本地bundled LibreOfficeKit实际更新域，配合已有中文字体渲染；**20/20页已逐页目视检查**，无观察到的缺字、截断、重叠或失效题注。目录**35/35项**与实际正文页一致；页脚依次为i–iv、1–16，保留1个TOC、26个STYLEREF、26个SEQ。
- 更新域后另存的DOCX再次严格审计：0错误、1条提示。提示为编辑器移除了自动刷新请求；本轮已经通过真实域更新、目录页码对照及页图独立核验处置，未伪造机器视觉完成状态。

本轮最终证据版本（生成结果和页图保留在本地忽略的`build/srs-final/`）：

| 对象 | SHA-256 |
|---|---|
| 输入模型JSON | `a0440de58331ad233ccdf4b0df534b95126929da02c38b215e946abd9be2480c` |
| 更新域前DOCX | `4fe96b54bb4ab38aaac0933b5b453c4aa051de6b81c9a5b10f671ace80af2673` |
| 更新域后最终DOCX | `5e50b8e3d53173a77d15d301ce3db0f68ecf5ea14888cb3099036edcaae3cc2b` |
| 同次导出的PDF | `7ff870300c33b5bd03c442a7bdabeac4bb79dcc65d5c97bbd5d5c330844352e3` |

## 使用边界

此记录验证skill实现和明确范围内的虚构样例；不是实际软件产品验收、认证或正式项目评审。脚本只报告已实现的机器规则，人工语义及视觉检查未实际执行时仍为`not_run`；真实任务须重新核验来源真实性、适用性和最终保存版本。CI在Linux/Windows的Python 3.9、3.11及macOS的3.11执行回归，不自动替代人工语义和逐页检查。
